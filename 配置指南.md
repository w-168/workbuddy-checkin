# WorkBuddy 每日自动签到 - GitHub Actions 配置指南

> 目标：每天北京时间 09:00 自动领取「Buddy 加油站」100 积分，无需手动操作。

---

## 一、准备工作

### 1.1 确认登录态文件存在

WorkBuddy 桌面客户端登录后，登录态文件位于：

```
C:\Users\<你的用户名>\AppData\Local\CodeBuddyExtension\Data\Public\auth\workbuddy-desktop.info
```

验证方法（终端执行）：
```bash
ls "$HOME/AppData/Local/CodeBuddyExtension/Data/Public/auth/workbuddy-desktop.info"
```

如果文件不存在，请确保 WorkBuddy 桌面客户端已登录。

### 1.2 准备 GitHub 账号

- 登录 [github.com](https://github.com)，注册/登录账号
- 进入 [https://github.com/77cinc/workbuddy-checkin](https://github.com/77cinc/workbuddy-checkin) 点击 **Fork** 按钮，fork 到自己的账号下

---

## 二、克隆并修改项目

### 2.1 克隆 fork 后的仓库

```bash
git clone https://github.com/你的用户名/workbuddy-checkin.git
cd workbuddy-checkin
```

### 2.2 删除原仓库的自动签到适配器代码

保留 fork 的原始文件，删除 `adapters/workbuddy.py` 中的旧实现，替换为以下内容：

**新建 `adapters/workbuddy.py`：**

```python
"""WorkBuddy Buddy加油站 每日签到适配器。

接口来源：WorkBuddy 桌面客户端逆向
  POST https://www.codebuddy.cn/v2/billing/meter/daily-checkin
  Headers:
    Authorization: Bearer {accessToken}
    X-User-Id:     {uid}
    X-Domain:      www.codebuddy.cn
    Content-Type:  application/json
  Body: {}

  返回 code=0   → 签到成功（附 credit / streak_days）
  返回 code=10001 + msg="今天已签到" → 今日已领，幂等跳过

登录态文件路径（Windows）：
  C:/Users/<username>/AppData/Local/CodeBuddyExtension/Data/Public/auth/workbuddy-desktop.info
"""
from __future__ import annotations

import json
import logging
import os
from urllib import error, request

from adapters.base import BaseAdapter, CheckinResult

logger = logging.getLogger(__name__)

# ---------- 配置 ----------
BASE_URL = "https://www.codebuddy.cn"
CHECKIN_PATH = "/v2/billing/meter/daily-checkin"
STATUS_PATH = "/v2/billing/meter/checkin-activity-status"
TIMEOUT = 25
USER_AGENT = "WorkBuddy/5.4.4"

# 登录态源文件（动态拼接，避免硬编码 Windows 用户名；仅本地模式使用）
AUTH_SRC_WIN = os.path.join(
    os.environ.get("LOCALAPPDATA") or os.path.expanduser(r"~\AppData\Local"),
    "CodeBuddyExtension", "Data", "Public", "auth", "workbuddy-desktop.info",
)
# 镜像副本：本地由 sync_auth.py 生成；CI 上由 GitHub Secret 注入
AUTH_MIRROR = os.path.join(os.path.dirname(os.path.abspath(__file__)), ".auth-info.json")


def _in_ci() -> bool:
    """是否运行在 CI 环境（GitHub Actions / 通用 CI）。"""
    return os.environ.get("GITHUB_ACTIONS") == "true" or os.environ.get("CI") == "true"


def _auth_candidates() -> list[str]:
    """按运行环境返回登录态候选路径。CI 下只认 Secret 注入的镜像文件。"""
    if _in_ci():
        return [AUTH_MIRROR]
    return [AUTH_SRC_WIN, AUTH_MIRROR]


def _load_auth() -> tuple[str, str, str]:
    """返回 (accessToken, uid, domain)。"""
    candidates = _auth_candidates()
    for path in candidates:
        if os.path.exists(path):
            try:
                with open(path, encoding="utf-8") as f:
                    info = json.load(f)
                auth = info.get("auth") or {}
                account = info.get("account") or {}
                token = auth.get("accessToken")
                uid = account.get("uid")
                domain = auth.get("domain") or "www.codebuddy.cn"
                if token and uid:
                    logger.info("登录态加载成功（来源: %s）", path)
                    return token, uid, domain
            except Exception as e:
                logger.warning("登录态文件读取失败 %s: %s", path, e)
    raise RuntimeError(
        "登录态不可用！\n"
        "请确保 WorkBuddy 桌面客户端已登录，且文件存在于:\n"
        f"  {AUTH_SRC_WIN}\n"
        "或手动执行同步: python sync_auth.py"
    )


def _post(path: str, token: str, uid: str, domain: str) -> tuple[int, dict]:
    url = BASE_URL + path
    req = request.Request(
        url,
        data=b"{}",
        method="POST",
        headers={
            "Authorization": f"Bearer {token}",
            "X-User-Id": uid,
            "X-Domain": domain,
            "Content-Type": "application/json",
            "Accept": "application/json",
            "User-Agent": USER_AGENT,
        },
    )
    try:
        with request.urlopen(req, timeout=TIMEOUT) as r:
            return r.status, json.loads(r.read().decode("utf-8", "ignore"))
    except error.HTTPError as e:
        try:
            body = json.loads(e.read().decode("utf-8", "ignore"))
        except Exception:
            body = {}
        return e.code, body
    except Exception as e:
        return -1, {"msg": f"{type(e).__name__}: {e}"}


class WorkBuddyAdapter(BaseAdapter):
    name = "workbuddy"

    def enabled(self) -> bool:
        # 有登录态文件才启用（CI 下只判断 Secret 注入的镜像文件）
        return any(os.path.exists(p) for p in _auth_candidates())

    def checkin(self) -> CheckinResult:
        try:
            token, uid, domain = _load_auth()
        except RuntimeError as e:
            return CheckinResult(self.name, False, None, str(e))

        # ① 执行签到
        status, body = _post(CHECKIN_PATH, token, uid, domain)
        code = body.get("code")
        msg = body.get("msg", "")

        if status == 200 and code == 0:
            credit = body.get("credit")
            streak = body.get("streak_days")
            points = credit if credit is not None else None
            parts = ["签到成功"]
            if points is not None:
                parts.append(f"获得 {points} 积分")
            if streak is not None:
                parts.append(f"连续 {streak} 天")
            return CheckinResult(
                self.name, True, points,
                "，".join(parts),
                raw=json.dumps(body, ensure_ascii=False)[:300],
            )

        # 今日已签到，幂等跳过
        if "今天已签到" in msg or code == 10001:
            # 补查一次状态用于汇报连续天数
            s2, b2 = _post(STATUS_PATH, token, uid, domain)
            streak = total = None
            if s2 == 200 and b2.get("code") == 0:
                d = b2.get("data") or {}
                streak = d.get("streak_days")
                total = d.get("total_credits")
            extra = ""
            if streak is not None:
                extra = f"，已连续 {streak} 天"
            if total is not None:
                extra += f"，累计 {total} 积分"
            return CheckinResult(
                self.name, True, None,
                f"今日已签到，跳过{extra}",
            )

        # 其他异常
        return CheckinResult(
            self.name, False, None,
            f"签到失败 code={code} msg={msg}",
            raw=json.dumps(body, ensure_ascii=False)[:300],
        )
```

---

## 三、添加辅助脚本

### 3.1 新建 `sync_auth.py`

用于将登录态从客户端复制到项目目录：

```python
"""同步 WorkBuddy 桌面客户端登录态到项目目录。

用法:
  python sync_auth.py              # 从客户端源文件同步
  python sync_auth.py --dry-run    # 仅验证源文件存在，不写镜像
"""
from __future__ import annotations

import json
import os
import sys

# 动态拼接，避免在公开仓库中硬编码 Windows 用户名
AUTH_SRC = os.path.join(
    os.environ.get("LOCALAPPDATA") or os.path.expanduser(r"~\AppData\Local"),
    "CodeBuddyExtension", "Data", "Public", "auth", "workbuddy-desktop.info",
)
AUTH_MIRROR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "adapters", ".auth-info.json")


def main() -> int:
    dry_run = "--dry-run" in sys.argv

    if not os.path.exists(AUTH_SRC):
        print(f"错误: 登录态源文件不存在: {AUTH_SRC}")
        print("请确保 WorkBuddy 桌面客户端已登录。")
        return 1

    if dry_run:
        print(f"[DRY-RUN] 源文件存在: {AUTH_SRC}")
        with open(AUTH_SRC, encoding="utf-8") as f:
            data = json.load(f)
        token = data.get("auth", {}).get("accessToken", "")
        uid = data.get("account", {}).get("uid", "")
        print(f"  accessToken: {token[:20]}...")
        print(f"  uid: {uid}")
        print(f"  domain: {data.get('auth', {}).get('domain', 'www.codebuddy.cn')}")
        return 0

    # 写入镜像（权限 600，仅自己可读写）
    with open(AUTH_SRC, encoding="utf-8") as f:
        content = f.read()
    json.loads(content)  # 校验 JSON 格式

    os.makedirs(os.path.dirname(AUTH_MIRROR), exist_ok=True)
    with open(AUTH_MIRROR, "w", encoding="utf-8") as f:
        f.write(content)
    os.chmod(AUTH_MIRROR, 0o600)

    print(f"✅ 登录态已同步到: {AUTH_MIRROR}")
    print("   注意：此文件含 accessToken，请勿提交到 Git 仓库！")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

### 3.2 新建 `.gitignore`

```
adapters/.auth-info.json
__pycache__/
*.pyc
*.pyo
.env
```

---

## 四、配置 GitHub Actions 工作流

### 4.1 新建 `.github/workflows/checkin.yml`

```yaml
name: WorkBuddy 每日签到

on:
  schedule:
    # 每天北京时间 09:00 触发（UTC 01:00）
    - cron: '0 1 * * *'
  workflow_dispatch:  # 允许手动触发
  push:               # 推送即跑一次，便于验证；稳定后可删除
    branches: [main]

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 恢复登录态（仅使用 GitHub Secret）
        env:
          AUTH_INFO: ${{ secrets.WORKBUDDY_AUTH_INFO }}
        run: |
          set -euo pipefail
          if [ -z "${AUTH_INFO:-}" ]; then
            echo "::error::未配置 Secret WORKBUDDY_AUTH_INFO，无法签到"
            exit 1
          fi
          mkdir -p adapters
          printf '%s' "$AUTH_INFO" > adapters/.auth-info.json
          chmod 600 adapters/.auth-info.json
          # 校验 JSON 合法且含必需字段，避免 Secret 粘错时静默失败
          # （用独立脚本而非内联 heredoc：YAML 缩进会让 heredoc 结束标记带上
          #   空格，bash 会报 "delimited by end-of-file" 导致步骤直接语法错误）
          python check_auth.py adapters/.auth-info.json

      - name: 执行签到
        env:
          SERVERCHAN_SCKEY: ${{ secrets.SERVERCHAN_SCKEY }}
          BARK_URL: ${{ secrets.BARK_URL }}
          NOTIFY_WEBHOOK: ${{ secrets.NOTIFY_WEBHOOK }}
        run: |
          set -o pipefail
          # tee 会吞掉退出码，用 PIPESTATUS[0] 取回真实结果
          python run_local.py 2>&1 | tee checkin.log
          exit "${PIPESTATUS[0]}"

      - name: 上传日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkin-log
          path: checkin.log
          if-no-files-found: warn
          retention-days: 7
```

> 关键点：登录态**完全来自 GitHub Secret**，工作流不会访问你电脑上的任何文件，
> 所以电脑关机、不开机都不影响定时任务。签到失败时工作流会正确标记为失败（红色）。

---

## 五、配置 GitHub Secrets

### 5.1 同步本地登录态

```bash
cd /path/to/workbuddy-checkin
python sync_auth.py
```

### 5.2 获取登录态内容

```bash
python -c "import json,os; print(open('adapters/.auth-info.json',encoding='utf-8').read())"
```

### 5.3 添加 Secret

1. 打开仓库页面 → **Settings** → **Secrets and variables** → **Actions**
2. 点 **New repository secret**
3. 添加以下密钥：

| Secret 名称 | 值 |
|------------|-----|
| `WORKBUDDY_AUTH_INFO` | 上一步输出的完整 JSON 字符串 |
| `SERVERCHAN_SCKEY`（可选） | 去 [sct.ftqq.com](https://sct.ftqq.com) 注册获取 |
| `BARK_URL`（可选） | Bark iOS 推送地址，如 `https://api.day.app/YOUR_TOKEN` |
| `NOTIFY_WEBHOOK`（可选） | 企业微信/钉钉机器人 Webhook 地址 |

---

## 六、推送代码

```bash
git add -A
git commit -m "feat: 配置 WorkBuddy 每日自动签到"
git push origin main
```

---

## 七、手动触发测试

1. 打开 [GitHub Actions 页面](https://github.com/你的用户名/workbuddy-checkin/actions)
2. 点击 **WorkBuddy 每日签到** 工作流
3. 点 **Run workflow** → **Run workflow**
4. 等待完成，查看日志确认签到结果

---

## 八、后续维护

### 8.1 刷新登录态（约每 60 天一次）

当 accessToken 过期时，GitHub Actions 会报"登录态不可用"错误。此时需要：

1. 打开 WorkBuddy 桌面客户端，确认已登录
2. 重新同步登录态：
   ```bash
   python sync_auth.py
   ```
3. 在 GitHub Secrets 中更新 `WORKBUDDY_AUTH_INFO`

### 8.2 查看运行历史

访问 [GitHub Actions 页面](https://github.com/你的用户名/workbuddy-checkin/actions) 查看每次运行的状态和日志。

---

## 九、常见问题

### Q: 为什么服务器找不到登录态文件？

A: `adapters/workbuddy.py` 里的路径必须与 `sync_auth.py` 完全一致。Windows 下正确路径为：
```
C:\Users\<你的用户名>\AppData\Local\CodeBuddyExtension\Data\Public\auth\workbuddy-desktop.info
```

### Q: 如何修改签到时间？

A: 编辑 `.github/workflows/checkin.yml`，修改 cron 表达式。例如每天 10:00 改为：
```yaml
- cron: '0 2 * * *'  # UTC 02:00 = 北京 10:00
```

### Q: 签到了但没收到推送通知？

A: 检查是否配置了推送渠道 Secret（`SERVERCHAN_SCKEY` / `BARK_URL` / `NOTIFY_WEBHOOK`），未配置时结果只输出到日志。
